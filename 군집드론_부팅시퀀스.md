# 군집 드론 부팅 시퀀스

**대상 스택:** Jetson Orin NX · ROS2 · ArduPilot · Aerostack2 · 리더-팔로워 구조

---

## 0. 시스템 아키텍처 개요

각 기체(droneX)는 동일한 2층 구조를 갖는다.

```
┌──────────────────────────────────────────────┐
│  Jetson Orin NX  (Ubuntu / JetPack-L4T)        │
│  ┌──────────────────────────────────────────┐ │
│  │  Aerostack2 (ROS2 / namespace: droneX)     │ │
│  │   ├ as2_platform_mavlink   (플랫폼)        │ │
│  │   ├ as2_state_estimator     (상태추정)     │ │
│  │   ├ as2_motion_controller   (제어)         │ │
│  │   ├ as2_behaviors  (takeoff/land/goto/...) │ │
│  │   └ mission / formation node (앱 로직)     │ │
│  └────────────────┬─────────────────────────┘ │
│                   │ MAVROS (MAVLink ↔ ROS2)     │
└───────────────────┼────────────────────────────┘
                    │ UART/Serial (또는 Ethernet)
┌───────────────────┴────────────────────────────┐
│  Flight Controller (Pixhawk/Cube) — ArduPilot   │
│   EKF3 / 센서 / 모터 ESC 출력                    │
└─────────────────────────────────────────────────┘

기체 간 통신: ROS2 DDS (또는 Zenoh 브리지) over Wi-Fi/Mesh
```

- **ArduPilot(FC):** 저수준 비행제어·자세안정·EKF·세이프티 담당
- **MAVROS:** MAVLink ↔ ROS2 토픽 변환 브리지 (`/droneX/mavros/...`)
- **Aerostack2:** 플랫폼 독립적·모듈형·행동(Behavior) 기반 ROS2 프레임워크. ArduPilot은 `as2_platform_mavlink`(MAVROS 기반) 플러그인으로 연결
- **리더-팔로워 로직:** Aerostack2 행동 위에 얹는 애플리케이션 레이어 (프레임워크 기본 제공 X)

---

## 1단계 — FC 부팅 (ArduPilot)
- 부트로더 → ArduCopter 펌웨어 로드
- IMU/지자기/기압계/GPS 등 센서 초기화 및 자이로 바이어스 보정
- EKF3 상태 추정기 기동
- 동반 컴퓨터 연결용 시리얼 포트 활성화 (`SERIALx_PROTOCOL = 2 (MAVLink2)`)
- ⚠️ 보정 중 기체 정지 유지

## 2단계 — Jetson Orin NX 부팅
- Ubuntu(JetPack/L4T) 부팅 → systemd 서비스 기동
- ROS2 환경 로드 (`source /opt/ros/<distro>/setup.bash`, 워크스페이스 overlay)
- 시각 동기화 데몬(chrony/PTP) 기동 — **군집 동기화의 기반**
- 네트워크 인터페이스(Wi-Fi/Mesh) 활성화, `ROS_DOMAIN_ID` / DDS 설정 적용

## 3단계 — MAVROS 링크 확립
- MAVROS 노드 실행, FC와 시리얼 연결 (예: `fcu_url:=/dev/ttyTHS1:921600`)
- ArduPilot HEARTBEAT 수신 대기 → 연결 확인
- `/droneX/mavros/state` 가 connected=true 가 될 때까지 블록
- IMU/자세/GPS 데이터가 `/droneX/mavros/...` 토픽으로 스트리밍 시작
- 필요 시 `SYSID_MYGCS` 설정으로 MAVROS의 기체 제어 허용

## 4단계 — Aerostack2 스택 기동 (namespace: droneX)
런치 순서(의존성 기준):
1. **as2_platform_mavlink** — MAVROS 위에서 동작하는 플랫폼 인터페이스. arming/offboard·모드 전환·액추에이터 명령 추상화
2. **as2_state_estimator** — 자세/위치 추정값을 통합하여 `/droneX/self_localization/...` 발행
3. **as2_motion_controller** — 위치/속도/궤적 명령을 FC 제어 입력으로 변환
4. **as2_behaviors** — takeoff, land, go_to, follow_path, follow_reference 등 ROS2 Action 서버 등록
5. **mission/formation node** — 리더-팔로워 로직 (앱 레이어)

## 5단계 — 사전 점검 및 상태 추정 수렴
- ArduPilot Pre-arm Check (배터리, 센서 헬스, GPS Fix, EKF 분산, 페일세이프)
- EKF3 수렴 → 홈 위치(Home) 설정
- Aerostack2 플랫폼 상태가 `LANDED / READY`(또는 동등 상태)로 진입했는지 확인

## 6단계 — 군집 합류 및 리더-팔로워 협상
- 각 기체가 고유 namespace(drone0, drone1 …)로 DDS 디스커버리에 합류
- 기체 간 상태 공유: 리더의 pose/속도/궤적을 토픽으로 브로드캐스트
  - 동일 LAN 멀티캐스트 또는 **Zenoh-ROS2 브리지**(손실 많은 Wi-Fi/Mesh에 최적)
- 역할 배정: 리더 1기 + 팔로워 N기
  - 정적 배정(설정 파일) 또는 동적 합의(consensus)
- 팔로워는 리더 상대 **대형 오프셋(formation offset)** 파라미터 수신

## 7단계 — 임무 동기화 및 무장 대기
- 미션/대형 정의 로드 및 검증
- 모든 기체의 플랫폼 상태·EKF·링크가 정상인지 집계 ("All Ready" 게이트)
- arming 대기 (동기화 배리어)

## 8단계 — 동기화 시동 및 이륙
- 시동 명령 → `as2_platform_mavlink`가 ArduPilot arming + GUIDED/OFFBOARD 모드 진입
- **takeoff 행동(Action)** 을 군집 전체에 동시 트리거 (동기화된 시각 기준)
- 이륙 후 제어 루프 전환:
  - 리더: 미션 경로 추종(follow_path) 또는 수동/상위 명령
  - 팔로워: 리더 pose + 오프셋 → `go_to` / `follow_reference`로 대형 유지
  - 충돌 회피·대형 보정 루프 상시 동작

---

## 리더-팔로워 데이터 흐름 (런타임)

```
[Leader]  self_localization/pose ──┐ (DDS/Zenoh)
                                   ▼
[Follower] formation_node:  target = leader_pose ⊕ offset
                                   │
                                   ▼
            as2_behaviors (go_to / follow_reference)
                                   │
            as2_motion_controller → as2_platform_mavlink
                                   │  (MAVROS/MAVLink)
                                   ▼
                          ArduPilot (GUIDED) → 모터
```

---

## 부팅 순서 요약

| 구간 | 단계 | 핵심 |
|------|------|------|
| FC 초기화 | 1 | ArduPilot 부팅·EKF3·시리얼 |
| 컴퓨팅 초기화 | 2 | Jetson/ROS2/시각동기/네트워크 |
| 브리지 연결 | 3 | MAVROS ↔ ArduPilot HEARTBEAT |
| 프레임워크 기동 | 4 | Aerostack2 노드 런치 |
| 사전 점검 | 5 | Pre-arm + EKF 수렴 + Home |
| 군집 협조 | 6~7 | 합류·역할·대형·동기화 게이트 |
| 비행 | 8 | 동기 시동 → 동기 이륙 → 대형 유지 |

---

## 구현 시 주의점 (Gotchas)

- **시각 동기화 필수:** chrony/PTP 또는 GPS 시각으로 기체 클럭 정렬. 어긋나면 동시 이륙·대형이 깨진다.
- **DDS 디스커버리 부하:** 기체 수가 늘면 멀티캐스트 디스커버리가 Wi-Fi/Mesh에서 불안정 → Zenoh 브리지 또는 Discovery Server 권장.
- **namespace 일관성:** 모든 노드를 `/droneX` 네임스페이스로 통일해야 토픽 충돌이 없다.
- **ArduPilot ↔ ROS2 연동 방식 선택:**
  - MAVROS: 안정적인 레거시 경로 (현재 Aerostack2 `as2_platform_mavlink` 기준)
  - ArduPilot 4.5+ 직접 DDS: MAVROS 제거 가능하나 일부 기능만 지원 → 검증 후 채택
- **모드 매핑:** Aerostack2 offboard 제어는 ArduPilot GUIDED 모드와 대응. 페일세이프 모드 전환 시 행동 상태머신과 충돌 없는지 확인.
- **이륙 동기화:** 네트워크 지연을 고려해 "예약 시각 기반(timed) takeoff"가 즉시 명령보다 동기화에 유리.
