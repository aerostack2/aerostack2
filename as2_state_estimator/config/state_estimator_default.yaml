/**:
  ros__parameters:
    # The canonical frames of the robot are declared by as2::Node and shared by
    # every node, so this package declares no parameters of its own. The
    # placeholder keeps the file a valid params file for the launch helpers.
    null: null
